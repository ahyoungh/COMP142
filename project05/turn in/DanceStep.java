package project5;
/* Ahyoung Hwang - I have neither given nor received unauthorized aid on this program. */

/**
 * A DanceStep is a enumerated type (like a class, but simpler) that
 * represents a single step of a dance.  There are only seven possible steps.
 */
public enum DanceStep {
    FORWARD, BACKWARD, STEP_RIGHT, STEP_LEFT, TURN_RIGHT, TURN_LEFT, PAUSE
}
